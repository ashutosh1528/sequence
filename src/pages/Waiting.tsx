import socket from "../services/socket";

const WaitingPage = () => {
  // socket.on("playerJoined", (data) => {
  //   console.log("wating -- ", { data });
  // });
  return (
    <div>
      <div>Waiting route;</div>
    </div>
  );
};

export default WaitingPage;
